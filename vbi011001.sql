CREATE TABLE gold.vbi011001 (
    `cd_hansya` STRING,
    `cd_kaisya` STRING,
    `kb_keiyaku` STRING,
    `no_edaban` STRING,
    `nu_sotezert` STRING,
    `no_renban` STRING,
    `kj_riyoseim` STRING,
    `kj_kanyusyu` STRING,
    `totalcount` STRING
) USING PARQUET
;
