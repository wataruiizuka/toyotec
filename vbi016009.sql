CREATE TABLE gold.vbi016009 (
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `ソート順` STRING,
    `車名ソート順` STRING,
    `新車車名コード` STRING,
    `店舗短縮名称` STRING,
    `ゾーンコード` STRING,
    `ゾーン名称` STRING,
    `グレード` STRING,
    `品名カナ` STRING,
    `新車車名` STRING,
    `受注計上日` STRING,
    `台数` STRING
) USING PARQUET
;
