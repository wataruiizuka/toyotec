お疲れ様です。

READMEに沿って、`spark-job-runner` で `gold.vbi0020013` のDDL実行を進めています。

現時点の対応状況は以下です。

・サーバ上の配置先は `/home/cloudbreak/spark/spark/job_runner` で確認済みです。
・`config/jobs.yaml` は `vbi0020013` 実行用に修正済みです。
・DDL/SQLの参照先もサーバ上の実パスに修正済みです。
・`validate` は成功しています。
　`Validation PASSED: no errors found.`
・PyYAML不足、PySpark参照不可の問題は解消済みです。
・PySparkはCloudera Spark同梱のものを `PYTHONPATH` に追加して読み込める状態にしました。
・その後、`deploy-ddl` を実行したところ、SparkContext作成時にYARN/HDFS側のKerberos認証で停止しました。

現在のエラーは以下です。

`Client cannot authenticate via:[TOKEN, KERBEROS]`

確認したところ、Hadoop設定は Kerberos 認証必須になっていました。

`hadoop.security.authentication = kerberos`

また、現在のログインユーザーは `cloudbreak` ですが、`kinit cloudbreak` は以下の理由で失敗しています。

`Client 'cloudbreak@TOYOTECD.KJ09-SZPI.A0.CLOUDERA.SITE' not found in Kerberos database`

そのため、現時点では `cloudbreak` ユーザーではKerberos認証できず、Spark/YARNに接続できない状態です。

確認したい内容は以下です。

1. この環境でSparkジョブを実行する際に使用すべきKerberos principal は何でしょうか。
2. 実行用のkeytabはありますでしょうか。ある場合、利用可能なパスと利用ユーザーを教えてください。
3. `cloudbreak` ユーザーはSparkジョブ実行用ではなく、接続・作業用ユーザーという理解で合っていますでしょうか。
4. `spark-job-runner` 実行前に必要な `kinit` 手順、または既存の実行ラッパーがあれば教えてください。
5. 今回の `gold.vbi0020013` 作成は、`cloudbreak` から実行する想定で問題ないか、それとも別ユーザーで実行すべきか確認したいです。

こちらの理解では、現時点のブロッカーはDDL/SQLや `jobs.yaml` の問題ではなく、Spark/YARN実行前のKerberos認証情報不足です。
実行用principalまたはkeytabをご確認いただけますでしょうか。
