CREATE TABLE gold.vbi010001 (
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `店舗名称` STRING,
    `店舗短縮名称` STRING,
    `ソート順` STRING,
    `販社会社店舗` STRING,
    `ゾーン名称` STRING,
    `商品区分` STRING,
    `pn区分` STRING,
    `tl区分` STRING,
    `新U区分` STRING,
    `本部店舗区分` STRING
) USING PARQUET
;
