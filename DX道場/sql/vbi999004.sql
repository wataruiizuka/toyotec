CREATE TABLE gold.vbi999004 (
    `No.` STRING,
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `店舗名` STRING,
    `ストール番号` STRING,
    `ストール名称` STRING,
    `集計対象判別` STRING
) USING PARQUET
;
