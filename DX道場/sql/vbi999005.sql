CREATE TABLE gold.vbi999005 (
    `No.` STRING,
    `販社コード` STRING,
    `会社コード` STRING,
    `車名コード` STRING,
    `車名（漢字）` STRING,
    `車名（カナ）` STRING,
    `表示可否` STRING,
    `ソート順` STRING
) USING PARQUET
;
