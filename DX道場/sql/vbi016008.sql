CREATE TABLE gold.vbi016008 (
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `下取有無` STRING,
    `下取車の車名` STRING,
    `新車車名` STRING,
    `受注計上日` STRING,
    `店舗短縮名称` STRING,
    `ゾーンコード` STRING,
    `ゾーン名称` STRING,
    `下取車の自社他社` STRING,
    `下取車の年式` STRING,
    `ソート順` STRING,
    `車名ソート順` STRING,
    `台数` STRING,
    `下取台数` STRING
) USING PARQUET
;
