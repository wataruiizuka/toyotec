CREATE TABLE gold.vbi016007 (
    `販社コード` STRING,
    `会社コード` STRING,
    `ゾーンコード` STRING,
    `ゾーン名称` STRING,
    `店舗コード` STRING,
    `ソート順` STRING,
    `車名ソート順` STRING,
    `新車車名コード` STRING,
    `店舗短縮名称` STRING,
    `法人個人区分` STRING,
    `年齢別` STRING,
    `購入形態` STRING,
    `新車車名` STRING,
    `受注計上日` STRING,
    `商談動機` STRING,
    `支払区分` STRING,
    `台数` STRING
) USING PARQUET
;
