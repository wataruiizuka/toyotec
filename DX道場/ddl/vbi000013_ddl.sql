CREATE TABLE gold.vbi000013 (
    `ソート順` STRING,
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `店舗名称` STRING,
    `品番` STRING,
    `品名` STRING,
    `在庫数` STRING
) USING PARQUET
;
