CREATE TABLE gold.vbi013011 (
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `注文no` STRING,
    `注文枝番` STRING,
    `pk_販社会社注文no枝番` STRING,
    `販社会社店舗コード` STRING,
    `店舗略称` STRING,
    `エリア統括部` STRING,
    `ゾーンコード` STRING,
    `ソート順` STRING,
    `登録` STRING,
    `納車` STRING,
    `売掛残高` STRING,
    `社員名` STRING,
    `売上経過日数` STRING,
    `買主名` STRING,
    `名義人` STRING,
    `契約形態` STRING,
    `注文NO_枝番` STRING,
    `型式` STRING,
    `受注日` STRING,
    `申込金受領日` STRING,
    `登録日` STRING,
    `納車日` STRING,
    `回収予定日` STRING,
    `最終入金日（現金）` STRING,
    `最終入金日（割賦）` STRING,
    `下取完了日` STRING,
    `完了` STRING,
    `売掛金残高合計` STRING,
    `現金残高` STRING,
    `割賦残高` STRING,
    `下取車残高` STRING,
    `申込金` STRING,
    `現金入金回数` STRING,
    `カード入金回数` STRING,
    `振当-納車` STRING,
    `登録-納車` STRING,
    `登録-完了` STRING,
    `払出区分` STRING,
    `法人個人区分` STRING,
    `発生累計` STRING,
    `更新日時` STRING,
    `納車～完了` STRING
) USING PARQUET
;
