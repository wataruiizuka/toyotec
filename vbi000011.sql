CREATE TABLE gold.vbi000011 (
    `販社コード` STRING,
    `会社コード` STRING,
    `店舗コード` STRING,
    `取替部品品番` STRING,
    `整理ＮＯ` STRING,
    `フレーム型式` STRING,
    `支払区分` STRING,
    `販社会社整理ＮＯフレーム型式支払区分` STRING,
    `取替部品個数` STRING,
    `取替部品品名` STRING
) USING PARQUET
;
