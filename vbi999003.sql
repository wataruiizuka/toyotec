CREATE TABLE gold.vbi999003 (
    `No.` STRING,
    `販社コード` STRING,
    `会社コード` STRING,
    `新車中古車区分` STRING,
    `リードタイム日` STRING,
    `未納車経過日` STRING,
    `登録経過日` STRING,
    `振当経過日` STRING,
    `受注経過日` STRING
) USING PARQUET
;
