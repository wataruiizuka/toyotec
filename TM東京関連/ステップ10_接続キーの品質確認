SELECT
    COUNT(*) AS row_count,

    SUM(
        CASE
            WHEN no_frame IS NULL THEN 1
            ELSE 0
        END
    ) AS null_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) = ''
            THEN 1
            ELSE 0
        END
    ) AS blank_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN 1
            ELSE 0
        END
    ) AS nonblank_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND no_frame <> TRIM(no_frame)
            THEN 1
            ELSE 0
        END
    ) AS outer_space_count,

    COUNT(
        DISTINCT CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN TRIM(no_frame)
            ELSE NULL
        END
    ) AS distinct_trimmed_no_frame_count,

    MIN(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN LENGTH(TRIM(no_frame))
            ELSE NULL
        END
    ) AS min_no_frame_length,

    MAX(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN LENGTH(TRIM(no_frame))
            ELSE NULL
        END
    ) AS max_no_frame_length

FROM ai21rep_pr_kudu.tbaz017m;

row_count	null_no_frame_count	blank_no_frame_count	nonblank_no_frame_count	outer_space_count	distinct_trimmed_no_frame_count	min_no_frame_length	max_no_frame_length
23708578	0	11059	23697519	23706506	4467375	1	21

SELECT
    COUNT(*) AS row_count,

    SUM(
        CASE
            WHEN no_frame IS NULL THEN 1
            ELSE 0
        END
    ) AS null_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) = ''
            THEN 1
            ELSE 0
        END
    ) AS blank_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN 1
            ELSE 0
        END
    ) AS nonblank_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND no_frame <> TRIM(no_frame)
            THEN 1
            ELSE 0
        END
    ) AS outer_space_count,

    COUNT(
        DISTINCT CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN TRIM(no_frame)
            ELSE NULL
        END
    ) AS distinct_trimmed_no_frame_count,

    MIN(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN LENGTH(TRIM(no_frame))
            ELSE NULL
        END
    ) AS min_no_frame_length,

    MAX(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN LENGTH(TRIM(no_frame))
            ELSE NULL
        END
    ) AS max_no_frame_length

FROM ai21rep_pr_kudu.tbba001g;

row_count	null_no_frame_count	blank_no_frame_count	nonblank_no_frame_count	outer_space_count	distinct_trimmed_no_frame_count	min_no_frame_length	max_no_frame_length
3361888	0	493108	2868780	506239	1322794	1	7

SELECT
    COUNT(*) AS row_count,

    SUM(
        CASE
            WHEN no_frame IS NULL THEN 1
            ELSE 0
        END
    ) AS null_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) = ''
            THEN 1
            ELSE 0
        END
    ) AS blank_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN 1
            ELSE 0
        END
    ) AS nonblank_no_frame_count,

    SUM(
        CASE
            WHEN no_frame IS NOT NULL
             AND no_frame <> TRIM(no_frame)
            THEN 1
            ELSE 0
        END
    ) AS outer_space_count,

    COUNT(
        DISTINCT CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN TRIM(no_frame)
            ELSE NULL
        END
    ) AS distinct_trimmed_no_frame_count,

    MIN(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN LENGTH(TRIM(no_frame))
            ELSE NULL
        END
    ) AS min_no_frame_length,

    MAX(
        CASE
            WHEN no_frame IS NOT NULL
             AND TRIM(no_frame) <> ''
            THEN LENGTH(TRIM(no_frame))
            ELSE NULL
        END
    ) AS max_no_frame_length

FROM ai21rep_pr_kudu.tbba085g;

row_count	null_no_frame_count	blank_no_frame_count	nonblank_no_frame_count	outer_space_count	distinct_trimmed_no_frame_count	min_no_frame_length	max_no_frame_length
2209291	0	0	2209291	11377	1102279	1	7
