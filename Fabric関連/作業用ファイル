SELECT
  COUNT(*) AS row_count,
  COUNT(DISTINCT CONCAT(
    COALESCE(cd_hansya, ''),
    '|',
    COALESCE(cd_kaisya, ''),
    '|',
    COALESCE(no_cyumon, ''),
    '|',
    COALESCE(no_cyumoned, '')
  )) AS distinct_key_count
FROM ai21rep_ve_stg_kudu.tbba001g;
